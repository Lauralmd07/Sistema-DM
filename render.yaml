services:
  - type: web
    name: sistema-dm
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn gridfs_app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
